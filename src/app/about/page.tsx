import React from 'react'
import { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'About Page'
};

const page = () => {
  return (
    <div>
        about
      
    </div>
  )
}

export default page
